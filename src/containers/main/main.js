import React, { Component } from 'react';
import FormsList from '../forms/formsList';

class Main extends Component {
    render () {
      return (
        <div>
          <h1>Main</h1>
          <FormsList />
        </div>
      );
    }
}

export default Main;
